pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout the repository
                git url: 'https://github.com/VrushilPatel/HWE.git', branch: 'main'
            }
        }

        stage('Build') {
            steps {
                git url: 'https://github.com/VrushilPatel/HWE.git', branch: 'main'
                
            }
        }

        stage('Test') {
            steps {
                git url: 'https://github.com/VrushilPatel/HWE.git', branch: 'main'
                
            }
        }
    }
}
